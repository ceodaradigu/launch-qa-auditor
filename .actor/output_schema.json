{
  "actorOutputSchemaVersion": 1,
  "title": "Launch QA Acceptance Oracle output",
  "description": "Prioritized launch findings plus optional deterministic acceptance criteria and evidence for every audited public page.",
  "properties": {
    "results": {
      "type": "string",
      "title": "Launch QA results",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
